server:
  port: 8000

spring:
  application:
    name: nacos-consumer
  cloud:
    nacos:
      discovery:
        # nacos地址
        server-addr: localhost:8848

management:
  endpoints:
    web:
      exposure:
        include: '*'

service-url:
  nacos-user-service: http://nacos-provider



